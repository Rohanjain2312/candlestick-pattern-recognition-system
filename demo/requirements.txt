# Trimmed dependency set for the Hugging Face Space (free CPU tier).
# Deliberately excludes the dataset-build stack (pyarrow, scikit-learn, pytest)
# and pins torch to the CPU wheel so the Space image stays small enough to build.
--extra-index-url https://download.pytorch.org/whl/cpu
gradio>=5.0
ultralytics>=8.3
torch>=2.2
numpy>=1.26
pandas>=2.0
matplotlib>=3.8
mplfinance>=0.12.10b0
yfinance>=0.2.60
huggingface_hub>=0.25
TA-Lib>=0.6
