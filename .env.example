# Copy this file to `.env` and fill in both values. `.env` is gitignored.
# GITHUB_TOKEN: github.com/settings/tokens -> classic token with `repo` scope
GITHUB_TOKEN=
# HF_TOKEN: huggingface.co/settings/tokens -> token with `Write` scope
HF_TOKEN=
